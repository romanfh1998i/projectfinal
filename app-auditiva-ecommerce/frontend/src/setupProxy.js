/**
 * This file is used by Create React App to customize the development server.
 * It's automatically recognized and used by react-scripts.
 */
module.exports = function(app) {
  // This function is called by webpack-dev-server
  // We don't need to do anything here, but the presence of this file
  // helps override the default webpack-dev-server configuration
  // and can prevent the deprecation warnings
};